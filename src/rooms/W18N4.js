// Forage room

import { sustain } from '../tasks';

const room = 'W17N4';

const unitCount = {
  harvesters: 6,
  foragers: 8,
  builders: 11,
  guards: 9,
  warriors: 0
};

export default () => {
	// sustain(room, unitCount);
};
